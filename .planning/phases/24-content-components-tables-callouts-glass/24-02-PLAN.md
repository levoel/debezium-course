---
phase: 24-content-components-tables-callouts-glass
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/Callout.tsx
autonomous: true

must_haves:
  truths:
    - "Callout components render with glass effect (backdrop-blur-sm)"
    - "Each callout type has distinct colored glass tint (blue/green/yellow/red)"
    - "Left border accent reinforces callout type visually"
    - "Callout text remains readable against glass background"
  artifacts:
    - path: "src/components/Callout.tsx"
      provides: "Glass-styled callout component with type-specific tints"
      exports: ["default"]
  key_links:
    - from: "src/components/Callout.tsx"
      to: "MDX content files"
      via: "import in MDX"
      pattern: "import Callout"
---

<objective>
Enhance Callout component with glass effect and type-specific colored glass tints while maintaining readability.

Purpose: Callouts benefit from glass aesthetic while maintaining semantic color-coding for accessibility (info=blue, tip=green, warning=yellow, danger=red).
Output: Updated Callout.tsx with backdrop-blur-sm and type-specific glass tints.
</objective>

<execution_context>
@/Users/levoely/.claude/get-shit-done/workflows/execute-plan.md
@/Users/levoely/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-content-components-tables-callouts-glass/24-RESEARCH.md
@src/components/Callout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add glass effect to Callout component</name>
  <files>src/components/Callout.tsx</files>
  <action>
Update the Callout.tsx component to use glass effect with type-specific colored tints.

Replace the styles object with glass-enhanced versions:
```tsx
const styles = {
  note: {
    bg: 'bg-blue-500/10 backdrop-blur-sm',  // Glass tint blue
    border: 'border-l-4 border-blue-500',
    icon: 'i',
    title: 'Note',
  },
  tip: {
    bg: 'bg-green-500/10 backdrop-blur-sm',  // Glass tint green
    border: 'border-l-4 border-green-500',
    icon: 'lightbulb',
    title: 'Tip',
  },
  warning: {
    bg: 'bg-yellow-500/10 backdrop-blur-sm',  // Glass tint yellow
    border: 'border-l-4 border-yellow-500',
    icon: 'warning',
    title: 'Warning',
  },
  danger: {
    bg: 'bg-red-500/10 backdrop-blur-sm',  // Glass tint red
    border: 'border-l-4 border-red-500',
    icon: 'alert',
    title: 'Danger',
  },
};
```

Update the container div className to add glass styling:
```tsx
<div className={`my-6 p-4 rounded-xl ${style.bg} ${style.border}`}>
```

Key changes:
- Replace dark theme solid backgrounds (bg-*-950/30) with glass tints (bg-*-500/10)
- Add backdrop-blur-sm to each style.bg
- Keep border-l-4 accent border
- Update border-radius from rounded-lg to rounded-xl for consistency with glass design
- Keep emoji icons (already working)

Full updated component:
```tsx
import React from 'react';

interface CalloutProps {
  type?: 'note' | 'tip' | 'warning' | 'danger';
  children: React.ReactNode;
}

const Callout: React.FC<CalloutProps> = ({ type = 'note', children }) => {
  const styles = {
    note: {
      bg: 'bg-blue-500/10 backdrop-blur-sm',
      border: 'border-l-4 border-blue-500',
      icon: 'i',
      title: 'Note',
    },
    tip: {
      bg: 'bg-green-500/10 backdrop-blur-sm',
      border: 'border-l-4 border-green-500',
      icon: 'lightbulb',
      title: 'Tip',
    },
    warning: {
      bg: 'bg-yellow-500/10 backdrop-blur-sm',
      border: 'border-l-4 border-yellow-500',
      icon: 'warning',
      title: 'Warning',
    },
    danger: {
      bg: 'bg-red-500/10 backdrop-blur-sm',
      border: 'border-l-4 border-red-500',
      icon: 'alert',
      title: 'Danger',
    },
  };

  const style = styles[type];

  return (
    <div className={`my-6 p-4 rounded-xl ${style.bg} ${style.border}`}>
      <div className="flex items-start gap-3">
        <span className="text-xl flex-shrink-0 mt-0.5">{style.icon}</span>
        <div className="flex-1 min-w-0">
          <div className="font-semibold mb-2 text-gray-100">
            {style.title}
          </div>
          <div className="prose prose-sm prose-invert max-w-none">
            {children}
          </div>
        </div>
      </div>
    </div>
  );
};

export default Callout;
```

Wait - keep the emoji icons from the original:
- note: ℹ️
- tip: 💡
- warning: ⚠️
- danger: 🚨
  </action>
  <verify>
Run `npm run build` - build succeeds.
Find a lesson using Callout component and verify:
- Callout has visible glass blur effect
- Blue tint for note, green for tip, yellow for warning, red for danger
- Left border accent matches callout type color
- Text inside callout is readable
  </verify>
  <done>
Callout component renders with glass effect (backdrop-blur-sm), type-specific colored glass tints (blue/green/yellow/red), and left border accent. Text remains readable against glass background.
  </done>
</task>

</tasks>

<verification>
After task completes:
1. Run `npm run build && npm run preview`
2. Navigate to a lesson with callouts (search MDX files for `<Callout`)
3. Verify each callout type has:
   - Visible glass blur effect (backdrop-blur-sm)
   - Type-specific colored tint (note=blue, tip=green, warning=yellow, danger=red)
   - 4px left border accent in matching color
   - Readable text content
4. Test responsiveness on mobile viewport
</verification>

<success_criteria>
- [ ] Note callout has blue glass tint + blue left border
- [ ] Tip callout has green glass tint + green left border
- [ ] Warning callout has yellow glass tint + yellow left border
- [ ] Danger callout has red glass tint + red left border
- [ ] All callouts have visible glass blur (backdrop-blur-sm)
- [ ] Callout text is readable against glass background
- [ ] Build succeeds without errors
- [ ] Component maintains emoji icons
</success_criteria>

<output>
After completion, create `.planning/phases/24-content-components-tables-callouts-glass/24-02-SUMMARY.md`
</output>
